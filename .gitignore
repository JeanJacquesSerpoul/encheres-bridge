bids
bids.exe
bids-linux
bids-windows.exe
tools/par/out/
.env
.env.*
!.env.example

# cli/bids.wasm et cli/wasm_exec.js sont versionnés : une copie de cli/
# depuis GitHub doit suffire à faire tourner l'application. Le workflow
# wasm.yml les recompile à chaque modification des sources Go sur main.
cli/bids_wasm_bin.js

# Verrou local de session Claude Code (sessionId/pid propres à la machine)
.claude/scheduled_tasks.lock
